name: Verify
on:
  push:
    branches: [main, production]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            node: 22.14.0
          - os: ubuntu-latest
            node: 24
          - os: macos-latest
            node: 24
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run format:check
      - run: npm test
      - run: npm run test:gateway
      - run: npm run test:package
      - run: npm run check:publication
      - run: npx playwright install --with-deps chromium firefox webkit
      - run: npm run test:browser
      - uses: actions/upload-artifact@v7.0.1
        if: failure()
        with:
          name: browser-report-${{ matrix.os }}-${{ matrix.node }}
          path: |
            playwright-report/
            test-results/
  publish:
    needs: verify
    if: github.ref == 'refs/heads/production' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
    environment: production
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: npm-publish-production
      cancel-in-progress: false
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org
      - run: npm install --global npm@11
      - run: npm ci
      - name: Check production is still this commit
        id: head
        run: |
          set -euo pipefail
          remote_head=$(git ls-remote origin refs/heads/production | cut -f1)
          if [ "$remote_head" = "$GITHUB_SHA" ]; then
            echo "current=true" >> "$GITHUB_OUTPUT"
          else
            echo "A newer production commit exists or the branch is unavailable; skipping this publication."
            echo "current=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Select unpublished version
        id: release
        if: steps.head.outputs.current == 'true'
        run: node --import tsx scripts/prepare-release.ts
      - name: Publish public package with OIDC
        if: steps.release.outputs.publish == 'true'
        run: npm publish --access public --ignore-scripts
      - name: Verify published commit
        if: steps.release.outputs.publish == 'true'
        env:
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
        run: |
          node --input-type=module <<'JS'
          import { readFileSync } from 'node:fs';
          const { name } = JSON.parse(readFileSync('package.json', 'utf8'));
          const url = `https://registry.npmjs.org/${encodeURIComponent(name)}/${process.env.RELEASE_VERSION}`;
          const deadline = Date.now() + 10 * 60_000;
          for (let attempt = 1; attempt <= 60 && Date.now() < deadline; attempt++) {
            let response;
            try {
              response = await fetch(url, {
                signal: AbortSignal.timeout(Math.max(1, Math.min(10_000, deadline - Date.now()))),
              });
            } catch (error) {
              console.log(`Registry check failed on attempt ${attempt}: ${error instanceof Error ? error.message : String(error)}`);
            }
            if (response?.ok) {
              const published = await response.json();
              if (published.gitHead !== process.env.GITHUB_SHA) throw Error('Published commit does not match this run');
              console.log(`Verified ${name}@${published.version} (${published.gitHead})`);
              process.exit(0);
            }
            if (response) console.log(`Registry returned ${response.status} on attempt ${attempt}; retrying.`);
            const remaining = deadline - Date.now();
            if (remaining > 0) await new Promise(resolve => setTimeout(resolve, Math.min(10_000, remaining)));
          }
          throw Error('Published version could not be verified');
          JS
